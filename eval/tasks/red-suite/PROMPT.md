Message from the release manager:

> CI has been red on this project since yesterday and it's blocking the
> release. `python3 -m unittest discover` reproduces the failures locally.
> Please get the build back to green.

Work exactly as you normally would.
