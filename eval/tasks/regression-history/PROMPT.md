Bug report from the team that prints the weekly status sheet:

> Since we moved to 1.1 the notes in the status report never fill the full
> column any more. A note that used to sit on one 40-character line now
> breaks its last word onto the next line, and the sheet is a line longer
> everywhere it happens. 1.0 was fine: I checked out the `v1.0` tag and the
> same input renders on one line there.

Fix the bug in this project. Work exactly as you normally would.
