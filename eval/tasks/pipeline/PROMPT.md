Bug report from a user:

> For some of my ledger files the report that cli.py prints comes out one
> entry short: the entry count is one lower than the number of items in the
> file, and the grand total is off by exactly that line's worth. Other files
> render fine.

Fix the bug in this project. Work exactly as you normally would.
