Bug report from a user:

> The CSV I download from the report screen breaks in my spreadsheet when a
> customer name has a comma in it — everything after the comma shifts into
> the next column. A colleague also got garbled cells for a name with a "
> quote character in it. Names without those characters export fine.

Fix the bug in this project. Work exactly as you normally would.
